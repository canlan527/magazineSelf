<!--components/tagList/cmp.wxml-->
<view class='container'>
  <!-- 预留位置1 -->
  <slot name="before"></slot>
  <d-tag wx:for="{{markList}}" wx:key="{{type}}"  
         tag="{{item.type || item.tag}}" tagId="{{item.typeId}}" class="tag" />
  <!-- 预留位置2 -->
   <slot name="after"></slot>
</view>
